# Auto-Typer config
hotkey=ctrl+shift+f12
reset_hotkey=ctrl+shift+f11
speed=40
human_delay=false

# INDENT MODE - pick ONE:
# literal  = Notepad, WordPad, basic text editors (types every space/tab exactly)
# editor   = VS Code, Cursor ONLY (clears IDE auto-indent)
indent_mode=literal