# Auto-Typer config
hotkey=ctrl+shift+f12
reset_hotkey=ctrl+shift+f11
speed=30
human_delay=false

# paste_lines = pastes each line exactly (BEST for text editors)
# keys        = types char-by-char (old method)
type_method=paste_lines

# Only used when type_method=keys
indent_mode=literal